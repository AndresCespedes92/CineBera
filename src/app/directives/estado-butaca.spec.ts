import { EstadoButaca } from './estado-butaca';

describe('EstadoButaca', () => {
  it('should create an instance', () => {
    const directive = new EstadoButaca();
    expect(directive).toBeTruthy();
  });
});
